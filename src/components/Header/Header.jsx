import React from 'react';
import styles from './style.module.scss';

function Header() {
    return (
        <header>
            <h1 className={styles.header}>English Words</h1>
        </header>

    );
}

export default Header;